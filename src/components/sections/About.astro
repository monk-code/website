---
// About section component
---

<section id="about" class="about-section">
  <!-- Background Elements -->
  <div class="section-background">
    <div class="floating-shapes">
      <div class="shape shape-1"></div>
      <div class="shape shape-2"></div>
      <div class="shape shape-3"></div>
    </div>
  </div>
  
  <div class="content-container relative z-10">
    <!-- Section Header -->
    <div class="section-header fade-in">
      <h2 class="section-title">
        <span class="title-main">About</span>
        <span class="title-accent">Gregory</span>
      </h2>
    </div>
    
    <!-- Main Content -->
    <div class="about-content fade-in" style="animation-delay: 0.1s;">
      <p class="about-text">
        I'm <span class="name-highlight">Gregory</span>. I studied history before getting into code—still love a good history documentary. 
        Been doing frontend development for over 10 years now, mostly helping scale enterprise applications with Vue.js and modern tools.
      </p>
      
      <p class="about-text">
        I really enjoy what I do. There's something satisfying about making complex systems work smoothly and keeping code 
        maintainable for the long haul. I've learned that taking time to get things right pays off every time.
      </p>
      
      <p class="about-text">
        When I'm not coding, I'm probably listening to Thelonious Monk (hence MONKCODE) or diving into classical music. 
        Love catching MMA fights, getting lost in games like Bloodborne, or laughing at British comedy from the 2000s. 
        Always looking for good film and series recommendations too.
      </p>
      
      <p class="about-text">
        I believe in doing solid work and being someone people can rely on. If you need help with a project, I'd be happy to chat.
      </p>
    </div>
  </div>
</section>

<script>
  // Fade-in animation on scroll
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1
  }

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible')
      }
    })
  }, observerOptions)

  // Observe all fade-in elements
  document.addEventListener('DOMContentLoaded', () => {
    const fadeElements = document.querySelectorAll('.about-section .fade-in')
    fadeElements.forEach(el => observer.observe(el))
  })
</script>

<style>
.about-section {
  position: relative;
  padding: 80px 0;
  /* Allow page-level gradients to flow through */
}

/* Background Elements */
.section-background {
  position: absolute;
  inset: 0;
  z-index: 0;
}

.floating-shapes {
  position: absolute;
  inset: 0;
}

.shape {
  position: absolute;
  opacity: 0.08;
  animation: gentle-float 20s infinite ease-in-out;
}

.shape-1 {
  width: 200px;
  height: 200px;
  top: 10%;
  right: 10%;
  background: radial-gradient(circle, var(--color-primary) 0%, transparent 70%);
  border-radius: 50%;
  animation-delay: 0s;
}

.shape-2 {
  width: 150px;
  height: 150px;
  bottom: 20%;
  left: 5%;
  background: linear-gradient(45deg, var(--color-primary) 0%, transparent 60%);
  transform: rotate(45deg);
  animation-delay: 7s;
}

.shape-3 {
  width: 100px;
  height: 100px;
  top: 60%;
  right: 20%;
  border: 2px solid var(--color-primary);
  border-radius: 20px;
  transform: rotate(15deg);
  animation-delay: 14s;
}

@keyframes gentle-float {
  0%, 100% {
    transform: translateY(0px) translateX(0px);
  }
  33% {
    transform: translateY(-20px) translateX(10px);
  }
  66% {
    transform: translateY(10px) translateX(-10px);
  }
}

/* Section Header */
.section-header {
  text-align: center;
  margin-bottom: 48px;
  position: relative;
  z-index: 1;
}

.section-title {
  font-size: clamp(2rem, 4vw, 2.75rem);
  font-weight: 700;
  line-height: 1.1;
  font-family: var(--font-family-heading);
}

.title-main {
  color: var(--color-foreground);
}

.title-accent {
  color: var(--color-primary);
}

/* Main Content */
.about-content {
  position: relative;
  z-index: 1;
  max-width: 700px;
  margin: 0 auto;
}

.about-text {
  font-size: 1.125rem;
  line-height: 1.7;
  color: var(--color-muted-foreground);
  margin-bottom: 24px;
  text-align: center;
}

.about-text:last-child {
  margin-bottom: 0;
}

.name-highlight {
  color: var(--color-primary);
  font-weight: 600;
}

/* Responsive Design */
@media (max-width: 768px) {
  .about-section {
    padding: 60px 0;
  }
  
  .section-header {
    margin-bottom: 36px;
  }
  
  .about-text {
    font-size: 1rem;
    margin-bottom: 20px;
  }
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  .shape {
    animation: none;
  }
}
</style>