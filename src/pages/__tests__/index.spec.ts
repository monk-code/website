import { describe, expect, it } from 'vitest'

describe('Homepage', () => {
  it('should have tests for homepage functionality', () => {
    // TODO: Add proper tests once Tailwind CSS v4 testing issue is resolved
    // Currently there's a conflict between @tailwindcss/vite plugin and Astro component testing
    expect(true).toBe(true)
  })
})
